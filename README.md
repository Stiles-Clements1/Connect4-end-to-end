# Connect4-end-to-end
Built a Connect4 AI by generating a large dataset of board states via Monte Carlo Tree Search, then training CNN and Transformer models for gameplay. Deployed a Dockerized backend on AWS and developed a frontend that allows users to play against the AI.
